<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
    xmlns="http://xmlns.jcp.org/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
    http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">
    
    <!-- Unidad de persistencia para el proyecto -->
    <persistence-unit name="LPII_T1_Fuentes_IsacPU" transaction-type="RESOURCE_LOCAL">
        
        <!-- Proveedor JPA: Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        
        <!-- ============================================= -->
        <!-- 1. PROPIEDADES DE CONEXIÓN A MYSQL -->
        <!-- ============================================= -->
        <properties>
            <!-- URL de conexión a la BD -->
            <property name="javax.persistence.jdbc.url" 
                value="jdbc:mysql://localhost:3306/BD1_Fuentes?useSSL=false&amp;serverTimezone=UTC"/>
            
            <!-- Usuario de MySQL (cambiar si es diferente) -->
            <property name="javax.persistence.jdbc.user" value="root"/>
            
            <!-- Contraseña de MySQL (MODIFICA ESTA PARTE) -->
            <property name="javax.persistence.jdbc.password" value="993619263Jf*"/>
            
            <!-- Driver JDBC de MySQL -->
            <property name="javax.persistence.jdbc.driver" 
                value="com.mysql.cj.jdbc.Driver"/>
            
            <!-- ============================================= -->
            <!-- 2. PROPIEDADES DE HIBERNATE -->
            <!-- ============================================= -->
            
            <!-- Dialecto para MySQL 8 -->
            <property name="hibernate.dialect" 
                value="org.hibernate.dialect.MySQL8Dialect"/>
            
            <!-- Mostrar las consultas SQL en consola (útil para debug) -->
            <property name="hibernate.show_sql" value="true"/>
            
            <!-- Formatear las consultas SQL para mejor legibilidad -->
            <property name="hibernate.format_sql" value="true"/>
            
            <!-- Comportamiento de DDL: 
                 'update' = actualiza esquema si hay cambios
                 'create' = crea todo nuevo cada vez
                 'validate' = solo valida esquema
                 'none' = no hace nada -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            
            <!-- Usar comentarios en SQL generado -->
            <property name="hibernate.use_sql_comments" value="true"/>
            
            <!-- Configuración de conexión pool (opcional pero recomendado) -->
            <property name="hibernate.c3p0.min_size" value="5"/>
            <property name="hibernate.c3p0.max_size" value="20"/>
            <property name="hibernate.c3p0.timeout" value="300"/>
            <property name="hibernate.c3p0.max_statements" value="50"/>
            <property name="hibernate.c3p0.idle_test_period" value="3000"/>
        </properties>
        
    </persistence-unit>
</persistence>